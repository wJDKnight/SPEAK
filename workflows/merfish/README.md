# MERFISH workflow

Scope: MERFISH_25–MERFISH_29. After downloading the Figshare data package, H5AD
files, all derived gene sets, and the exact 47 representative genes are in
`examples/merfish/`. `get_gene_list.R`
regenerates these lists and writes the integrated R objects to
`examples/intermediates/merfish/`.
